{
  "name": "linkedin-profile-grader",
  "version": "0.1.0",
  "private": true,
  "description": "Paid-first LinkedIn profile audit using Stripe, Apify and OpenAI.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check lib/context-token.js && node --check api/create-checkout.js && node --check api/analyze.js && node --check test/analyze.test.js",
    "test": "node test/analyze.test.js"
  }
}
